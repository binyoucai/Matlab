%定义不动点迭代函数RootB三次开根，a0为初始值、e为误差值
%返回值a为近似三次求根数，k为迭代次数
%函数开始
function [ a,k ] =RootB(a0,e)
%根据方程变形，初始化a的值
a=(a0+1)^(1/3);
%初始化k值
k=1;
%不动点迭代算法开始
while abs(a0-a)>=e
    a0=a;
    a=(a0+1)^(1/3);
    k=k+1;
end
%不动点迭代算法结束
end
%函数结束
