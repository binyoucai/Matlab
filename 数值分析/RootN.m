%定义牛顿迭代法迭代函数RootN求三次开根，f为原函数、g为原函数f的导函数,e为误差值
%返回值x为近似三次求根数，k为迭代次数
%函数开始
function [ x,k ] =RootN(f,g,e)
%初始化x0的值
x0=1.5;
%根据牛顿迭代法初始化x的值
x=x0-f(x0)/g(x0);
%初始化k值
k=1;
%牛顿迭代算法开始
while abs(x-x0)>=e
    x0=x;
    x=x-f(x0)/g(x0);
    k=k+1;
end
%牛顿迭代算法结束
end
%函数结束

